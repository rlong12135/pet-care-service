# pet-care-service

Static landing page for a pet-care business, intended for deployment to a
separate Azure Static Web App.

## Local preview

Open `index.html` directly, or run:

```bash
npx serve .
```
